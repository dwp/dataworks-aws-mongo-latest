groups:
  - name: master
    jobs:
      - aws-mongo-latest-dev
      - aws-mongo-latest-qa
      - aws-mongo-latest-integration
      - aws-mongo-latest-preprod
      - aws-mongo-latest-prod

  - name: pull-request
    jobs:
      - aws-mongo-latest-pr

  - name: clear-dynamodb-row
    jobs:
      - clear-dynamodb-row-development
      - clear-dynamodb-row-qa
      - clear-dynamodb-row-integration
      - clear-dynamodb-row-preprod
      - clear-dynamodb-row-production

  - name: update-pipeline
    jobs:
      - update-pipeline

  - name: admin-start-cluster
    jobs:
      - dev-start-cluster
      - qa-start-cluster
      - int-start-cluster
      - preprod-start-cluster
      - prod-start-cluster

  - name: admin-stop-cluster
    jobs:
      - dev-stop-cluster
      - qa-stop-cluster
      - int-stop-cluster
      - preprod-stop-cluster
      - prod-stop-cluster

  - name: ami-test
    jobs:
      - apply-and-test-with-ami
