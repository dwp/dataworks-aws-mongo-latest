groups:
  - name: master
    jobs:
      - development
      - qa
  
  - name: pull-request
    jobs:
      - dataworks-aws-mongo-latest-pr
  
  - name: update-pipeline
    jobs:
      - update-pipeline
