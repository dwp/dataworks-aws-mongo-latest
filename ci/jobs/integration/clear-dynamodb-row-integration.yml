jobs:
  - name: clear-dynamodb-row-integration
    plan:
      - .: (( inject meta.plan.clear-dynamodb-row ))
        config:
          params:
            AWS_ROLE_ARN: arn:aws:iam::((aws_account.integration)):role/ci
            AWS_ACC: ((aws_account.integration))
            CORRELATION_ID: ingest_emr_scheduled_tasks_export_snapshots_to_crown_integration_incremental_157_incremental
            DATA_PRODUCT: MONGO_LATEST
